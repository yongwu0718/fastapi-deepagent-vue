# 环境变量

提示

如果你已经知道什么是“环境变量”以及如何使用它们，可以跳过本节。

环境变量（也称为“**env var**”）是存在于 Python 代码**之外**、即**操作系统**中的变量，可以被你的 Python 代码（或其他程序）读取。

环境变量在处理应用程序**设置**、作为 Python **安装**的一部分等方面非常有用。

## 创建和使用环境变量

你可以在 **shell（终端）**中创建和使用环境变量，而无需使用 Python。

Windows PowerShell
```python
Create an env var MY_NAME
$Env:MY_NAME = "Wade Wilson"  

Use it with other programs, like
$echo "Hello $Env:MY_NAME"  

Hello Wade Wilson
```
## 在 Python 中读取环境变量

你也可以在 Python **之外**的终端中（或通过其他任何方式）创建环境变量，然后在 **Python 中读取它们**。

例如，你可以创建一个 `main.py` 文件：

```python
import os 
name = os.getenv("MY_NAME", "World") 
print(f"Hello {name} from Python")
```

提示

`os.getenv()` 的第二个参数是返回的默认值。

如果未提供，默认值为 `None`。在这里，我们提供 `"World"` 作为默认值。

然后你可以这样调用该 Python 程序：

Windows PowerShell

```python
💬 Here we don't set the env var yet
python main.py  

💬 As we didn't set the env var, we get the default value  
Hello World from Python  
  
💬 But if we create an environment variable first
$Env:MY_NAME = "Wade Wilson"  

💬 And then call the program again
python main.py  

💬 Now it can read the environment variable  
Hello Wade Wilson from Python

```

由于环境变量可以在代码之外设置，但可以被代码读取，且无需与文件一起存储（即不需要提交到 `git`），因此通常将它们用于配置或**设置**。

你也可以仅为**特定程序的调用**创建一个环境变量，该变量仅对该程序及其运行期间有效。

为此，请直接在程序命令之前，在同一行上创建它：

```python
💬 Create an env var MY_NAME in line for this program call
MY_NAME="Wade Wilson" python main.py  

💬 Now it can read the environment variable  
Hello Wade Wilson from Python  
  
💬 The env var no longer exists afterwards
python main.py  

Hello World from Python
```


提示

你可以在 The Twelve-Factor App: Config（十二要素应用：配置） 中阅读更多相关信息。

## 类型与验证

这些环境变量只能处理**文本字符串**，因为它们是 Python 外部的，必须兼容其他程序和系统的其余部分（甚至包括不同的操作系统，如 Linux、Windows 和 macOS）。

这意味着在 Python 中从环境变量读取的**任何值都将是 `str`（字符串）**，任何转换为其他类型或进行验证的操作都必须在代码中完成。

你将在 [高级用户指南 - 设置和环境变量] 中了解更多关于使用环境变量处理**应用程序设置**的内容。

## `PATH` 环境变量

有一个**特殊**的环境变量叫做 **`PATH`**，它被操作系统（Linux, macOS, Windows）用于查找要运行的程序。

`PATH` 变量的值是一个长字符串，由多个目录组成。在 Linux 和 macOS 上目录之间用冒号 `:` 分隔，在 Windows 上用分号 `;` 分隔。

例如，`PATH` 环境变量可能如下所示：

Windows

```bash
C:\Program Files\Python312\Scripts;C:\Program Files\Python312;C:\Windows\System32
```

这意味着系统应该在以下目录中查找程序：

- `C:\Program Files\Python312\Scripts`
- `C:\Program Files\Python312`
- `C:\Windows\System32`

当你在终端输入**命令**时，操作系统会**在 `PATH` 环境变量中列出的每个目录中**查找该程序。

例如，当你在终端输入 `python` 时，操作系统会在该列表的**第一个目录**中查找名为 `python` 的程序。

如果找到了，它就会**使用它**。否则，它会继续在**其他目录**中查找。

### 安装 Python 并更新 `PATH`

当你安装 Python 时，系统可能会询问你是否要更新 `PATH` 环境变量。

Windows

假设你安装了 Python，并且它位于 `C:\opt\custompython\bin` 目录下。

如果你选择更新 `PATH` 环境变量，安装程序会将 `C:\opt\custompython\bin` 添加到 `PATH` 中。

```bash
C:\Program Files\Python312\Scripts;
C:\Program Files\Python312;C:\Windows\System32;C:\opt\custompython\bin
```

这样，当你在终端输入 `python` 时，系统会在 `C:\opt\custompython\bin`（最后一个目录）中找到 Python 程序并使用它。

所以，如果你输入：

python  

Windows

系统会**在** `C:\opt\custompython\bin\python` 中找到 `python` 程序并运行它。

这大致等同于输入：

```bash
C:\opt\custompython\bin\python  
```

此信息在学习[虚拟环境]时非常有用。

## 结论

通过本节内容，你应该对什么是**环境变量**以及如何在 Python 中使用它们有了基本的了解。

你也可以在 维基百科关于环境变量的条目 中了解更多信息。

在很多情况下，环境变量如何发挥作用并不显而易见。但它们在开发过程中会出现在许多不同的场景中，因此了解它们很有必要。

例如，在关于虚拟环境的下一节中，你将需要用到这些知识。